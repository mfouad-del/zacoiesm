# نظام إدارة الهندسة المتكامل - تعليمات التشغيل
# Integrated Engineering Management System - Setup Instructions

## خطوات التشغيل / Setup Steps

### 1. تشغيل قاعدة البيانات / Run Database Scripts

قم بتشغيل السكريبتات بالترتيب:
Run the scripts in order:

```bash
# في Supabase SQL Editor / In Supabase SQL Editor
1. scripts/001_create_tables.sql
2. scripts/002_row_level_security.sql
3. scripts/003_triggers_and_functions.sql
4. scripts/004_seed_data.sql
5. scripts/005_create_super_admin.sql
```

### 2. إنشاء حساب المدير الأول / Create First Admin Account

**الطريقة الأولى: التسجيل المباشر / Method 1: Direct Signup**

1. افتح صفحة تسجيل الدخول / Open the login page
2. استخدم البيانات التالية / Use these credentials:
   - **البريد الإلكتروني / Email:** `admin@iems.com`
   - **كلمة المرور / Password:** `Admin@123456`

3. بعد التسجيل، افتح Supabase SQL Editor وقم بتشغيل:
   After signup, open Supabase SQL Editor and run:
   ```sql
   SELECT promote_to_super_admin('admin@iems.com');
   ```

**الطريقة الثانية: من Supabase Dashboard / Method 2: From Supabase Dashboard**

1. اذهب إلى Supabase Dashboard → Authentication → Users
2. اضغط "Add user" → "Create new user"
3. أدخل:
   - Email: `admin@iems.com`
   - Password: `Admin@123456`
   - Auto Confirm User: ✓ (مفعل)
4. بعد إنشاء المستخدم، انسخ الـ UUID
5. افتح SQL Editor وقم بتشغيل:
   ```sql
   UPDATE public.user_profiles 
   SET role = 'super_admin', 
       full_name = 'مدير النظام',
       updated_at = now()
   WHERE id = 'PASTE_UUID_HERE';
   ```

### 3. تسجيل الدخول / Login

استخدم البيانات التالية للدخول:
Use these credentials to login:

```
📧 البريد الإلكتروني / Email: admin@iems.com
🔑 كلمة المرور / Password: Admin@123456
```

### 4. إنشاء مستخدمين جدد / Create New Users

بعد تسجيل الدخول كـ Super Admin:
After logging in as Super Admin:

1. اذهب إلى صفحة "المستخدمين" / Go to "Users" page
2. اضغط "إضافة مستخدم" / Click "Add User"
3. املأ البيانات واختر الدور (super_admin / admin / user)
4. سيتم إرسال بريد تأكيد للمستخدم الجديد

---

## المميزات / Features

✅ **لوحة التحكم / Dashboard**
- إحصائيات شاملة للمشاريع والمهام
- تتبع الميزانية والموارد
- رسوم بيانية للتقدم

✅ **إدارة المشاريع / Projects Management**
- إنشاء وتعديل المشاريع
- تتبع التقدم والحالة
- إدارة الميزانية والفريق

✅ **التخطيط / Planning**
- إدارة المهام بنظام Kanban
- تعيين المهام للمستخدمين
- متابعة المواعيد النهائية

✅ **إدارة المواقع / Sites Management**
- تسجيل مواقع العمل
- تتبع المشاريع لكل موقع
- معلومات الاتصال والموقع

✅ **المعدات / Equipment**
- جرد المعدات
- جدولة الصيانة
- تتبع الحالة والتوفر

✅ **الموارد / Resources**
- إدارة المخزون
- تنبيهات نقص المواد
- سجل المعاملات

✅ **التقارير / Reports**
- إنشاء تقارير مخصصة
- تصدير البيانات
- تحليل الأداء

✅ **المستخدمين / Users** (Super Admin فقط)
- إضافة مستخدمين جدد
- تعديل الأدوار والصلاحيات
- حذف المستخدمين

---

## الأدوار والصلاحيات / Roles & Permissions

### Super Admin
- جميع الصلاحيات / Full access
- إدارة المستخدمين / User management
- حذف وتعديل كل شيء / Delete and edit everything

### Admin
- إنشاء وتعديل البيانات / Create and edit data
- عرض التقارير / View reports
- لا يمكن إدارة المستخدمين / Cannot manage users

### User
- عرض البيانات / View data
- تعديل المهام المعينة له / Edit assigned tasks
- لا يمكن الحذف / Cannot delete

---

## الدعم الفني / Technical Support

إذا واجهت أي مشاكل:
If you encounter any issues:

1. تأكد من تشغيل جميع السكريبتات بالترتيب
2. تحقق من أن Supabase متصل بشكل صحيح
3. راجع سجلات الأخطاء في المتصفح (F12)

---

## ملاحظات أمنية / Security Notes

⚠️ **مهم:** قم بتغيير كلمة المرور الافتراضية فوراً بعد أول تسجيل دخول!
**Important:** Change the default password immediately after first login!

🔒 **حماية البيانات:**
- جميع البيانات محمية بـ Row Level Security (RLS)
- كلمات المرور مشفرة
- الجلسات آمنة مع HTTP-only cookies
```

```typescriptreact file="" isHidden
